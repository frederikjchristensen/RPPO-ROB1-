#include <iostream>
#include <list>
#include <string>
#include <cmath>

using namespace std;

int main() {

    list<int> firstlist;

    firstlist.push_back(1);
    firstlist.push_back(2);
    firstlist.push_back(3);

    list<int>::iterator it;

    for(it = firstlist.begin(); it != firstlist.begin(); it++){

    }

    return 0;
}